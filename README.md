# aby-whiteboard

Whiteboard demo built with React, TypeScript, and Vite. Media (images and videos) are rendered and transformed using WebGL. An overlay canvas is used to render additional elements (selection rectangle, placeholders, etc.)

## Getting started

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Preview

```bash
npm run preview
```
